# Hazard-curve worked example

Generated by `python scripts/hazard_curve_example.py`. One adaptive-IS run (tuned to the rarest intensity) is reused to reconstruct the **entire** mean and fractile hazard curve.

Full writeup, tables, and interpretation: [`SUMMARY.md`](SUMMARY.md).

Expected plots: `mean_hazard_curve.png`, `fractile_hazard_curves.png`, plus `summary.json`.
